#ifndef MAIN_H
#define MAIN_H

#include <stddef.h>
#include <stdio.h>
#include <stdlib.h>
#include <stdarg.h>


int string_print(va_list folder);
int print_char(va_list folder);
int _putchar(char c);
int _printf(const char *format, ...);

#endif
