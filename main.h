#ifndef MAIN_H
#define MAIN_H

#include <stddef.h>
#include <stdio.h>
#include <stdlib.h>
#include <stdarg.h>
#include <string.h>

int _length_m(char *);
int _mputchar(char mwrite);
int _printf(const char *format, ...);
int _stringcheck(char *sttp);

#endif
